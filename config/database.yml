default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: goodlogik_development

test:
  <<: *default
  database: goodlogik_test

production:
  <<: *default
  database: goodlogik_production
